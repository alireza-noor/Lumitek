# 🚀 راهنمای انتشار Lumitek 0.6 روی اینترنت (دامنه + سئو)

این راهنما دقیقاً مراحل انتشار سایت نسخه ۰.۶ روی یک دامنه + ثبت در گوگل را توضیح می‌دهد تا همه بتوانند با جستجوی «لومیتک» سایتت را پیدا کنند — بدون این که تو کاری بکنی.

---

## گزینه ۱: Netlify (پیشنهادی — ساده‌ترین، دامنه رایگان: `lumitek.netlify.app`)

1. به [netlify.com](https://netlify.com) برو و با ایمیل/گیت‌هاب ثبت‌نام کن.
2. از داشبورد **Add new site → Deploy manually** را بزن.
3. فایل `Lumitek_0.6.zip` را (بدون باز کردن!) داخل صفحه بکش و رها کن.
4. از **Site configuration → Change site name** اسم را به `lumitek` تغییر بده → دامنه می‌شود:
   ### `https://lumitek.netlify.app` ✨
5. PWA و Service Worker روی HTTPS نت‌لیفای بلافاصله کار می‌کند.

### 🌐 اتصال دامنه اختصاصی (مثلاً lumitek.ir یا lumitek.com)
1. دامنه را از یک رجیسترار ایرانی (ایرنیک/نیک) یا [Namecheap](https://namecheap.com) بخر.
2. در Netlify: **Domain management → Add a domain → Add existing domain** و دامنه را وارد کن.
3. رکوردهای DNS که نت‌لیفای نشان می‌دهد را در پنل دامنه ثبت کن:
   - `A` record → `75.2.60.5`
   - یا `CNAME` برای `www` → `lumitek.netlify.app`
4. صبر کن DNS propagate شود (چند دقیقه تا چند ساعت). گواهی SSL خودکار صادر می‌شود.
5. **⚠️ مهم:** بعد از فعال شدن دامنه، آدرس `SITE_URL` را در `sitemap.xml`، `robots.txt` و تگ‌های `canonical`/`og:url` صفحات از `lumitek.netlify.app` به دامنه جدید تغییر بده و دوباره deploy کن.

## گزینه ۲: Vercel (`lumitek.vercel.app`)
1. [vercel.com](https://vercel.com) → ثبت‌نام.
2. **Add New → Project →** پوشه پروژه را آپلود کن (یا از GitHub).
3. در تنظیمات، site name را `lumitek` بگذار. اتصال دامنه اختصاصی هم مثل بالا از بخش Domains انجام می‌شود.

## گزینه ۳: GitHub Pages (`lumitek.github.io`)
1. اکانت GitHub بساز؛ یک ریپو به اسم `lumitek.github.io` بساز.
2. محتویات پوشه را آپلود کن (`index.html` باید در ریشه باشد).
3. Settings → Pages → Branch: `main` → Save. برای دامنه اختصاصی، فایل `CNAME` با نام دامنه در ریشه بگذار.

---

## 🔍 بعد از انتشار: ثبت در گوگل (تا «لومیتک» سرچ شود)

1. **[Google Search Console](https://search.google.com/search-console)** → Add property → آدرس سایتت را بده.
2. مالکیت را تأیید کن (روش HTML tag: متا-تگ را در `<head>` فایل `index.html` بگذار و دوباره آپلود کن).
3. از منوی **Sitemaps** آدرس `https://دامنه-تو/sitemap.xml` را ثبت کن (فایل آماده است — همه ۳۹ صفحه مهم داخلش هست).
4. از **URL Inspection** آدرس اصلی را بده و **Request Indexing** بزن.
5. معمولاً بین ۱ تا ۷ روز سایت برای جستجوی «Lumitek» یا «لومیتک» ایندکس می‌شود.
6. برای سرعت بیشتر: لینک سایت را در پروفایل شبکه‌های اجتماعی بگذار (backlink اولیه).
7. **سئو داخلی از قبل آماده است:** عنوان و توضیحات فارسی با کلمه «لومیتک»، JSON-LD (WebSite + Organization)، Open Graph، canonical و sitemap کامل. نام سایت، لوگو و توضیحات در `manifest.json` هم برای نصب PWA به‌روز است.

## ⚙️ بعد از انتشار: آدرس دامنه را در پروژه به‌روز کن

دامنه پیش‌فرض فعلی `https://lumitek.netlify.app` است. اگر دامنه دیگری گرفتی، در این فایل‌ها عوضش کن:
- `sitemap.xml` → همه `<loc>` ها
- `robots.txt` → خط sitemap
- تگ‌های `canonical` و `og:url` در صفحات (سرچ کن `lumitek.netlify.app`)
- JSON-LD داخل `index.html`

## 💰 درآمدزایی (فعال‌سازی پرداخت واقعی)

| بخش | فایل | کار لازم |
|-----|------|----------|
| خرید سکه | `js/main.js` → `LUMITEK_PAY_CONFIG` | `gateway: "zarinpal"` + merchantId از [zarinpal.com](https://zarinpal.com) — خرید سکه الان داخل **فروشگاه → تب شارژ سکه** است |
| حمایت مالی | `pages/donate.html` → `LUMITEK_DONATE_CONFIG` | merchantId + آدرس کیف پول واقعی USDT/BTC |
| ورود گوگل واقعی | `js/auth.js` → `AUTH_CONFIG` | ساخت پروژه Firebase و قرار دادن config (راهنما داخل فایل) |
| هوش مصنوعی لومیتک | رایگان با موتور Pollinations (بدون کلید) | برای کیفیت بالاتر: در صفحه AI دکمه ⚙️ API → کلید OpenAI/OpenRouter/GLM |
| اشتراک VIP | فروشگاه → تب شارژ سکه | با فعال شدن درگاه، پرداخت خودکار وصل می‌شود |

## 💎 اقتصاد سکه (v0.6)

- شروع هر کاربر: **۱۰ سکه** | پاداش روزانه: **۱۰ سکه** (VIP: ۲۰)
- مصرف اصلی: **هوش مصنوعی لومیتک (۲ سکه در هر سوال)** + فروشگاه (آواتار ۸۰ تا ۶۵۰، اسلحه ۳۰۰ تا ۱۰۰۰، اسکین ۱۵۰ تا ۷۵۰، بوست XP)
- درآمد کاربر: بازی‌ها (هر بازی ۵ تا ۶۰ سکه)، پاداش روزانه، تماشای آگهی (۲۰ سکه در ساعت)
- تعادل: یک کاربر روزی با پاداش + چند بازی حدود ۴۰-۱۰۰ سکه جمع می‌کند؛ یعنی روزی ۱ تا ۵ سوال AI رایگان و هر چند روز یک آیتم فروشگاه — برای بیشتر، باید بازی کند یا شارژ بخرد.

## ✅ چک‌لیست نهایی قبل از اعلام به دوستان

- [ ] سایت روی HTTPS بالا می‌آید
- [ ] PWA نصب می‌شود (دکمه Install در صفحه دانلود فعال است)
- [ ] حالت آفلاین تست شد (اینترنت را قطع کن و صفحه را رفرش کن)
- [ ] هوش مصنوعی لومیتک جواب می‌دهد و ۲ سکه کم می‌کند
- [ ] خرید سکه از فروشگاه → تب شارژ سکه کار می‌کند
- [ ] sitemap در سرچ کنسول ثبت شد
- [ ] درگاه پرداخت/کیف پول واقعی جایگزین نمونه‌ها شد
